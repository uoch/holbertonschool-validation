# hadolint configuration
ignore:
  - DL3008  # Allow apt-get update without apt-get upgrade
  - DL3009  # Allow apt-get upgrade without apt-get update
  - DL3015  # Allow apt-get autoremove without apt-get clean

